2026-07-29
#TOP #Learning 
git
git is a program that lets me "version control" my projects.
why would i want to even version control?
so big mistakes can be undone and alternative versions of the code can be developed.
it is basically a save file, every time a change is commited to the project it is saved and tracked for future referece.

github
is a website based on git the hosts my project on the web and lets me collab with other developers


==rools==

commit often
commit messages should be clear
dont edit on github


#### [Cheatsheet](https://www.theodinproject.com/lessons/foundations-git-basics#cheatsheet)

This is a reference list of the most commonly used Git commands. (You might consider bookmarking this handy page.) Try to familiarize yourself with the commands so that you can eventually remember them all:

- Commands related to a remote repository:
    - `git clone git@github.com:USER-NAME/REPOSITORY-NAME.git`
    - `git push` or `git push origin main` (Both accomplish the same goal in this context)
- Commands related to the workflow:
    - `git add .`
    - `git commit -m "A message describing what you have done to make this snapshot different"`
- Commands related to checking status or log history
    - `git status`
    - `git log`